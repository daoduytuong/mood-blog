# Nháp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cho phép tác giả lưu một bài chưa đăng (`is_published = false`), sửa lại sau — kể cả sửa ảnh — rồi tự chọn thời điểm đăng.

**Architecture:** Nháp là row `posts` với `is_published = false`; RLS sẵn có (`posts_public_read using (is_published)`) đã khiến nháp vô hình với mọi đường đọc công khai nên **không cần migration**. Nháp định danh bằng `id` qua route riêng `/me/nhap/[id]` (`force-dynamic`), dùng lại `ComposeForm` thay vì mở rộng `EditForm` — nhờ vậy bất biến "bài đã đăng không đổi ảnh" được giữ theo **cấu trúc route** chứ không theo cờ điều kiện. Slug tạm dạng `nhap-<base36>`, slug thật sinh lúc Đăng.

**Tech Stack:** Next.js 16.2.7 (App Router, Server Actions), React 19.2.4, Supabase (`@supabase/ssr`), Tailwind v4, TypeScript.

**Spec:** `docs/superpowers/plans/../specs/2026-09-11-nhap-design.md`

## Global Constraints

- **KHÔNG dùng TDD.** Dự án cố ý không có test framework (0 dependency test, không có script `test`). Mỗi task kết bằng ba lệnh gate + kiểm tay cụ thể, thay cho vòng RED-GREEN.
- **Definition of Done mỗi task:** `npm run lint` sạch · `npx tsc --noEmit` sạch · `npm run build` pass — **chạy thật và đọc output**, không được tuyên bố xong khi chưa chạy.
- Mọi lệnh chạy trong `mood-blog/`. Máy sau proxy công ty: dev server dùng `npm run dev:local`.
- **KHÔNG migration.** Không thêm cột, không thêm policy.
- Gọi Supabase **CHỈ** qua `src/lib/db/`. Không `supabase.from()` trong component/feature.
- Nút/ô nhập/chip **phải** dùng primitive `src/components/ui/{Button,Input,Textarea,Field,Chip}`. Biến thể qua **prop**, không chồng qua `className`.
- Không hardcode hex tâm trạng — luôn qua `src/lib/moods.ts`.
- Không spinner quay (`Button` đã xử lý: disabled + đổi chữ). Hover chỉ đổi màu/viền.
- Chữ hiển thị **tiếng Việt**, định danh code **tiếng Anh**.
- Ràng buộc DB đang có: `goc_doc_has_source check (type <> 'goc_doc' or (link_url is not null or excerpt is not null))` — nháp Góc đọc **buộc** phải có link hoặc đoạn trích, nếu không `insert` sẽ lỗi ở DB.

## Phạm vi — quyết định ở tầng plan (spec không nói)

**Nháp không hỗ trợ Khoảnh khắc-video.** Video chỉ là dán link Vimeo, không có gì để lắng lại; hỗ trợ nó đòi gọi `fetchVimeoMeta` lúc lưu nháp rồi dựng lại URL từ `video_id` khi mở lại form. Nút "Lưu nháp" **không hiện** khi `momentKind === "video"`.

**Nháp Hành trình chỉ có chặng đầu.** `addJourneyEntry` / `updateJourneyEntry` / `removeJourneyEntry` dùng `getBySlug` (lọc published) và **không sửa** — thêm chặng vẫn là việc sau khi đăng, đúng spec.

---

### Task 1: Tầng dữ liệu + sửa `deletePostAction` đọc theo id

**Files:**
- Modify: `src/lib/db/posts.ts` (thêm `slugExists`, `getByIdForAuthor`, `publishPost`; `NewPost` thêm `isPublished`)
- Modify: `src/features/compose/actions.ts` (`uniqueSlug` dùng `slugExists`; `deletePostAction` đọc theo id)

**Interfaces:**
- Consumes: `toPost`, `DB`, `Post` (đã có trong `posts.ts`)
- Produces:
  - `slugExists(sb: DB, slug: string): Promise<boolean>`
  - `getByIdForAuthor(sb: DB, id: string): Promise<Post | null>`
  - `publishPost(sb: DB, id: string, slug: string): Promise<void>`
  - `NewPost.isPublished?: boolean`

- [ ] **Step 1: Thêm ba hàm vào `src/lib/db/posts.ts`**

Đặt ngay sau `getBySlug` (nhóm các hàm đọc cạnh nhau):

```ts
/**
 * Slug này đã có ai chiếm chưa — KHÔNG lọc `is_published`.
 *
 * `getBySlug` lọc `is_published = true` nên không thấy nháp; dùng nó để kiểm
 * trùng slug sẽ sinh ra slug đã bị một nháp chiếm. Giới hạn đã biết: chạy bằng
 * client của tác giả nên RLS chỉ cho thấy nháp CỦA CHÍNH tác giả — blog một
 * người nên không sao, và ràng buộc `slug unique` ở DB vẫn là chốt cuối.
 */
export async function slugExists(sb: DB, slug: string): Promise<boolean> {
  const { data, error } = await sb
    .from("posts")
    .select("id")
    .eq("slug", slug)
    .maybeSingle();
  if (error) return false; // lỗi -> coi như trống; unique constraint chặn nốt
  return !!data;
}

/** Một bài của Tác giả kể cả NHÁP (RLS posts_author_all lo quyền). */
export async function getByIdForAuthor(
  sb: DB,
  id: string,
): Promise<Post | null> {
  const { data, error } = await sb
    .from("posts")
    .select("*")
    .eq("id", id)
    .maybeSingle();
  if (error || !data) return null;
  return toPost(data);
}

/**
 * Đăng một nháp: đổi slug + bật is_published.
 * Hàm DUY NHẤT được sửa hai cột này — cố ý không nhồi vào `updatePost` để
 * trong codebase không tồn tại đường nào đổi slug của bài ĐÃ đăng.
 */
export async function publishPost(
  sb: DB,
  id: string,
  slug: string,
): Promise<void> {
  const { error } = await sb
    .from("posts")
    .update({ slug, is_published: true })
    .eq("id", id);
  if (error) throw error;
}
```

- [ ] **Step 2: Cho `createPost` nhận `isPublished`**

Trong `src/lib/db/posts.ts`, thêm field vào `NewPost`:

```ts
export interface NewPost {
  authorId: string;
  type: PostType;
  mood: MoodCode;
  slug: string;
  caption?: string | null;
  excerpt?: string | null;
  linkUrl?: string | null;
  media?: MediaItem[];
  /** Bỏ trống = đăng ngay (mặc định DB). `false` = lưu nháp. */
  isPublished?: boolean;
}
```

và trong `createPost`, thêm một dòng vào object `.insert({...})`, ngay sau `media`:

```ts
      media: input.media ?? [],
      is_published: input.isPublished ?? true,
```

- [ ] **Step 3: `uniqueSlug` chuyển sang `slugExists`**

Trong `src/features/compose/actions.ts`, sửa import:

```ts
import {
  createPost,
  getBySlug,
  getByIdForAuthor,
  publishPost,
  slugExists,
  updatePost,
  deletePost,
} from "@/lib/db/posts";
```

và sửa thân `uniqueSlug` — chỉ đổi điều kiện vòng `for`:

```ts
  const base = slugify(hint) || `${fallbackPrefix}-${Date.now().toString(36)}`;
  let slug = base;
  for (let i = 2; await slugExists(supabase, slug); i++) slug = `${base}-${i}`;
  return slug;
```

- [ ] **Step 4: `deletePostAction` đọc theo id**

Thay toàn bộ thân hàm `deletePostAction` trong `src/features/compose/actions.ts`:

```ts
// Story 1.7 — xoá bài (hearts cascade theo FK -> tổng tim /me tự rụng).
// Đọc theo ID, KHÔNG theo slug: `getBySlug` lọc `is_published = true` nên bản
// cũ không xoá nổi NHÁP. Đọc theo id cũng bớt mong manh khi dọn Storage.
export async function deletePostAction(formData: FormData): Promise<void> {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) redirect("/login");

  const id = String(formData.get("id") ?? "");
  if (!id) redirect("/");

  const existing = await getByIdForAuthor(supabase, id);
  if (!existing || existing.authorId !== user.id) redirect("/");

  const wasPublished = existing.isPublished;
  const slug = existing.slug;

  // Dọn ảnh ở Storage (best-effort) trước khi xoá hàng — tránh rác.
  const paths = existing.media
    .map((m) => m.path)
    .filter((p): p is string => !!p);
  if (paths.length) await supabase.storage.from("media").remove(paths);

  try {
    await deletePost(supabase, id); // RLS đảm bảo chỉ author xoá được
  } catch {
    // Xoá hụt -> quay lại chỗ vừa đứng, không nuốt lỗi âm thầm.
    redirect(wasPublished ? `/m/${slug}` : "/me");
  }

  // Nháp chưa từng nằm trên trang công khai nào -> không cần revalidate.
  if (wasPublished) {
    revalidatePath(`/m/${slug}`);
    revalidatePath("/");
    redirect("/");
  }
  redirect("/me");
}
```

- [ ] **Step 5: Chạy gate**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

Kỳ vọng: cả ba sạch. `getBySlug` vẫn được dùng ở các chỗ khác nên **không** được xoá import của nó.

- [ ] **Step 6: Kiểm hồi quy bằng tay (bắt buộc — task này sửa đường xoá bài đang chạy)**

`npm run dev:local`, đăng nhập, rồi:

1. Mở một bài đã đăng → bấm Xoá → phải về `/` và bài biến mất khỏi feed.
2. Đăng một bài mới → slug vẫn sinh đúng từ caption (kiểm `uniqueSlug` không vỡ).

- [ ] **Step 7: Commit**

```bash
git add src/lib/db/posts.ts src/features/compose/actions.ts
git commit -m "feat(db): slugExists/getByIdForAuthor/publishPost + deletePostAction doc theo id"
```

---

### Task 2: Lưu nháp từ trang soạn + khu "Nháp" ở /me

**Files:**
- Modify: `src/features/compose/actions.ts` (thêm `saveDraft`)
- Modify: `src/features/compose/ComposeForm.tsx` (nút "Lưu nháp")
- Create: `src/features/drafts/DraftList.tsx`
- Modify: `src/app/me/page.tsx` (tách published/nháp; stats; MemoriesSection)

**Interfaces:**
- Consumes: `slugExists`/`createPost` (Task 1), `sanitizeImageMedia`, `sanitizeEntryMeta`, `uniqueSlug`, `ComposeState`
- Produces:
  - `saveDraft(prev: ComposeState, formData: FormData): Promise<ComposeState>`
  - `DraftList({ drafts }: { drafts: Post[] })` — server component

- [ ] **Step 1: Thêm `saveDraft` vào `src/features/compose/actions.ts`**

Đặt trước `createMomentImages`. Cần thêm `import type { PostType } from "@/lib/db/types";` vào đầu file (cùng dòng với `MediaItem`: `import type { MediaItem, PostType } from "@/lib/db/types";`).

```ts
// ===== NHÁP =====
// Nháp = post is_published=false. RLS `posts_public_read using (is_published)`
// đã khiến nó vô hình với mọi đường đọc công khai -> không cần migration.

const DRAFT_TYPES: PostType[] = ["khoanh_khac", "goc_doc", "hanh_trinh"];

/** Lưu nháp: bài chưa đăng, slug TẠM. Slug thật sinh lúc Đăng. */
export async function saveDraft(
  _prev: ComposeState,
  formData: FormData,
): Promise<ComposeState> {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return { error: "Bạn cần đăng nhập đã nhé." };

  const type = String(formData.get("type") ?? "") as PostType;
  if (!DRAFT_TYPES.includes(type)) return { error: "Loại bài không hợp lệ." };

  const mood = String(formData.get("mood") ?? "");
  if (!MOOD_CODES.includes(mood as MoodCode))
    return { error: "Chọn một tâm trạng giúp mình nhé." };

  const caption = String(formData.get("caption") ?? "").trim();
  const excerpt = String(formData.get("excerpt") ?? "").trim();
  const linkUrl = String(formData.get("linkUrl") ?? "").trim();

  // Góc đọc: ràng buộc DB `goc_doc_has_source` đòi có link HOẶC trích, kể cả
  // khi còn là nháp -> chặn ở đây để không ăn lỗi insert khó hiểu.
  if (type === "goc_doc" && !linkUrl && !excerpt)
    return { error: "Thêm một link hoặc đoạn trích nhé." };

  let media: MediaItem[] = [];
  try {
    media = sanitizeImageMedia(
      JSON.parse(String(formData.get("media") ?? "[]")),
      user.id,
    );
  } catch {
    media = [];
  }
  if (media.length > MAX_IMAGES) media = media.slice(0, MAX_IMAGES);

  // Hành trình: chặng đầu mang ngày + ghi chú (như createJourney).
  if (type === "hanh_trinh" && media[0]) {
    const meta = sanitizeEntryMeta(formData.get("note"), formData.get("date"));
    media = [{ ...media[0], ...meta }];
  }

  // Hint RỖNG -> luôn ra dạng `nhap-<base36>`. Cố ý KHÔNG truyền caption: slug
  // sẽ sinh lại lúc đăng, sinh theo caption ở đây chỉ tạo hai slug cho một bài.
  const slug = await uniqueSlug(supabase, "", "nhap");
  try {
    await createPost(supabase, {
      authorId: user.id,
      type,
      mood: mood as MoodCode,
      slug,
      caption: caption || null,
      excerpt: excerpt || null,
      linkUrl: linkUrl || null,
      media,
      isPublished: false,
    });
  } catch {
    const paths = media.map((m) => m.path).filter((p): p is string => !!p);
    if (paths.length) await supabase.storage.from("media").remove(paths);
    return { error: "Chưa lưu được nháp, thử lại nhé." };
  }

  // KHÔNG revalidatePath: nháp không lên trang công khai nào, và /me là
  // force-dynamic nên tự tươi.
  redirect("/me");
}
```

- [ ] **Step 2: Nút "Lưu nháp" trong `ComposeForm`**

Trong `src/features/compose/ComposeForm.tsx`:

(a) thêm `saveDraft` vào import từ `./actions`, và thêm một `useActionState`:

```ts
  const [draftState, draftAction] = useActionState(saveDraft, initial);
```

(b) thêm `draftState.error` vào chuỗi `error`:

```ts
  const error =
    localError ??
    imagesState.error ??
    videoState.error ??
    gocDocState.error ??
    journeyState.error ??
    draftState.error;
```

(c) ở đầu `onSubmit`, đọc nút nào vừa bấm và rẽ nhánh **trước** mọi kiểm tra dành cho việc đăng (nháp được phép thiếu ảnh):

```ts
  async function onSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setLocalError(null);
    const form = e.currentTarget;
    if (!mood) return setLocalError("Chọn một tâm trạng giúp mình nhé.");

    const caption = fieldValue(form, "caption");

    // Nút nào vừa bấm: "Lưu nháp" hay "Đăng".
    const submitter = (e.nativeEvent as SubmitEvent)
      .submitter as HTMLButtonElement | null;

    if (submitter?.value === "draft") {
      // Nháp: ảnh là TUỲ CHỌN (chưa có ảnh vẫn lưu được).
      const media = images.length > 0 ? await uploadPicked(images) : [];
      if (media === null) return; // uploadPicked đã setLocalError
      const fd = new FormData();
      fd.set("type", type);
      fd.set("mood", mood);
      fd.set("caption", caption);
      fd.set("media", JSON.stringify(media));
      if (type === "goc_doc") {
        fd.set("linkUrl", fieldValue(form, "linkUrl"));
        fd.set("excerpt", fieldValue(form, "excerpt"));
      }
      if (type === "hanh_trinh") {
        fd.set("note", fieldValue(form, "entryNote"));
        fd.set("date", entryDate || localToday());
      }
      startTransition(() => draftAction(fd));
      return;
    }

    if (type === "khoanh_khac" && momentKind === "video") {
```

(phần còn lại của `onSubmit` giữ nguyên từ dòng `const videoUrl = ...`)

(d) thay khối `<Button type="submit">Đăng</Button>` ở cuối form bằng:

```tsx
      <div className="flex items-center gap-3">
        <Button
          type="submit"
          value="publish"
          loading={busy}
          loadingLabel="Đang lưu…"
        >
          Đăng
        </Button>
        {/* Video chỉ là dán link -> không có gì để lắng lại, không cần nháp. */}
        {!(type === "khoanh_khac" && momentKind === "video") && (
          <Button
            type="submit"
            value="draft"
            variant="secondary"
            disabled={busy}
          >
            Lưu nháp
          </Button>
        )}
      </div>
```

Lưu ý: bỏ `className="self-start"` cũ của nút Đăng — bọc trong `div` rồi nên không cần.

- [ ] **Step 3: Tạo `src/features/drafts/DraftList.tsx`**

```tsx
import Image from "next/image";
import Link from "next/link";
import type { Post } from "@/lib/db/posts";
import { mediaPublicUrl } from "@/lib/storage";
import { postThumb, postTitle, POST_TYPE_LABEL } from "@/lib/post-type";
import { formatPostDate } from "@/lib/date";
import { MoodLabel } from "@/components/post/MoodBar";

/**
 * Nháp trên /me — chưa đăng, chỉ tác giả thấy (RLS chặn anon).
 *
 * Không có nháp thì KHÔNG render gì: nhắc "bạn chẳng có nháp nào" là việc
 * không cần thiết (cùng tinh thần MemoriesSection).
 */
export function DraftList({ drafts }: { drafts: Post[] }) {
  if (drafts.length === 0) return null;

  return (
    <section className="mt-8">
      <h2 className="mb-3 font-serif text-lg font-medium text-text">
        Nháp <span className="text-sm text-text-muted">({drafts.length})</span>
      </h2>
      <ul className="flex flex-col divide-y divide-border overflow-hidden rounded-lg border border-border bg-surface">
        {drafts.map((post) => {
          const media = postThumb(post);
          const src = media?.path ? mediaPublicUrl(media.path) : null;
          return (
            <li key={post.id}>
              <Link
                href={`/me/nhap/${post.id}`}
                className="flex items-center gap-3 p-3 transition-colors hover:bg-background"
              >
                <div className="relative h-14 w-14 shrink-0 overflow-hidden rounded-sm bg-border/40">
                  {src && (
                    <Image
                      src={src}
                      alt=""
                      fill
                      sizes="56px"
                      className="object-cover"
                    />
                  )}
                </div>
                <div className="flex min-w-0 flex-col gap-1">
                  <p className="line-clamp-1 text-sm text-text">
                    {postTitle(post)}
                  </p>
                  <div className="flex items-center gap-2 text-xs text-text-muted">
                    <span>{POST_TYPE_LABEL[post.type]}</span>
                    <span>·</span>
                    <span>{formatPostDate(post.createdAt)}</span>
                  </div>
                  <MoodLabel mood={post.mood} />
                </div>
              </Link>
            </li>
          );
        })}
      </ul>
    </section>
  );
}
```

- [ ] **Step 4: Tách published/nháp trong `src/app/me/page.tsx`**

(a) thêm import:

```ts
import { DraftList } from "@/features/drafts/DraftList";
```

(b) thay khối nạp dữ liệu (ngay sau `if (!user) redirect(...)`):

```ts
  // listByAuthor trả MỌI bài (kể cả nháp) — tách ở đây, không sửa tầng db.
  const all = await listByAuthor(supabase, user.id);
  const posts = all.filter((p) => p.isPublished);
  const drafts = all.filter((p) => !p.isPublished);
  const postIds = posts.map((p) => p.id);
```

(phần `const [counts, recent, totalComments] = await Promise.all([...])` giữ nguyên — nó đã dùng `postIds`)

(c) chèn `<DraftList>` ngay **trước** khối `{posts.length === 0 ? (`:

```tsx
      {/* Nháp — chưa đăng, đặt trên lưới bài vì đây là việc còn dở. */}
      <DraftList drafts={drafts} />

```

(d) sửa điều kiện rỗng để nháp không bị coi là "chưa có gì": đổi

```tsx
      {posts.length === 0 ? (
```

thành

```tsx
      {posts.length === 0 && drafts.length === 0 ? (
```

`stats` và `<MemoriesSection posts={posts} />` **không cần sửa** — chúng đã dùng biến `posts`, giờ chỉ còn bài đã đăng. Kiểm lại bằng mắt rằng `MemoriesSection` nhận `posts` (không phải `all`).

- [ ] **Step 5: Chạy gate**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

- [ ] **Step 6: Kiểm bằng tay — bao gồm phép thử bảo mật**

`npm run dev:local`, đăng nhập:

1. `/compose` → chọn Khoảnh khắc, thêm 2 ảnh + caption + tâm trạng → bấm **Lưu nháp** → về `/me`, thấy khu "Nháp (1)".
2. `/me` — dải thống kê "N bài" **không** đổi (nháp không được tính).
3. Mở `/` ở tab khác → **không** thấy bài nháp.
4. Lấy slug tạm của nháp từ DB (Supabase dashboard, cột `slug`, dạng `nhap-…`). Mở **cửa sổ ẩn danh** (đã đăng xuất) vào `/m/<slug-tạm>` → phải **404**.
5. `/compose` → chọn Video → nút "Lưu nháp" **không hiện**.
6. `/compose` → chọn Góc đọc, không nhập link lẫn trích → Lưu nháp → báo "Thêm một link hoặc đoạn trích nhé."
7. Đăng một bài bình thường (bấm Đăng) → vẫn hoạt động như cũ.

- [ ] **Step 7: Commit**

```bash
git add src/features/compose/actions.ts src/features/compose/ComposeForm.tsx src/features/drafts/DraftList.tsx src/app/me/page.tsx
git commit -m "feat(nhap): luu nhap tu trang soan + khu Nhap tren /me"
```

---

### Task 3: Xoá nháp từ /me

**Files:**
- Modify: `src/features/drafts/DraftList.tsx` (nút Xoá mỗi dòng)

**Interfaces:**
- Consumes: `deletePostAction` (Task 1 — đã đọc theo id, redirect `/me` khi là nháp)
- Produces: không có interface mới

- [ ] **Step 1: Thêm nút Xoá vào mỗi dòng nháp**

Trong `src/features/drafts/DraftList.tsx`, thêm import:

```ts
import { deletePostAction } from "@/features/compose/actions";
import { Button } from "@/components/ui/Button";
```

Đổi `<li>` để nút Xoá nằm **ngoài** `<Link>` (không lồng control trong link):

```tsx
            <li key={post.id} className="flex items-center gap-2 pr-3">
              <Link
                href={`/me/nhap/${post.id}`}
                className="flex min-w-0 flex-1 items-center gap-3 p-3 transition-colors hover:bg-background"
              >
```

(giữ nguyên phần bên trong `<Link>`, đóng `</Link>` như cũ, rồi thêm ngay sau nó:)

```tsx
              <form action={deletePostAction}>
                <input type="hidden" name="id" value={post.id} />
                <Button type="submit" variant="danger" size="sm">
                  Xoá
                </Button>
              </form>
            </li>
```

Lưu ý: `deletePostAction` dọn ảnh Storage rồi xoá row, và redirect về `/me` vì `isPublished === false`. **Không** truyền `slug` — bản mới không dùng nữa.

- [ ] **Step 2: Chạy gate**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

- [ ] **Step 3: Kiểm bằng tay**

1. `/me` → bấm **Xoá** ở một dòng nháp → dòng biến mất, vẫn ở `/me`.
2. Supabase dashboard → Storage bucket `media`: ảnh của nháp đó **không còn**.
3. Xoá một bài **đã đăng** (từ trang bài) → vẫn về `/` như cũ (hồi quy).

- [ ] **Step 4: Commit**

```bash
git add src/features/drafts/DraftList.tsx
git commit -m "feat(nhap): xoa nhap tu /me (don ca anh tren Storage)"
```

---

### Task 4: Route sửa nháp + `ComposeForm` nạp nháp (chữ + tâm trạng)

Task này **chưa** sửa ảnh — ảnh để Task 5. `updateDraft` ở đây không truyền `media` nên ảnh cũ giữ nguyên nguyên vẹn.

**Files:**
- Create: `src/app/me/nhap/[id]/page.tsx`
- Modify: `src/features/compose/actions.ts` (thêm `updateDraft`)
- Modify: `src/features/compose/ComposeForm.tsx` (prop `draft`)

**Interfaces:**
- Consumes: `getByIdForAuthor` (Task 1), `saveDraft` (Task 2)
- Produces:
  - `updateDraft(prev: ComposeState, formData: FormData): Promise<ComposeState>`
  - `DraftInit` (export từ `ComposeForm.tsx`):
    ```ts
    export interface DraftInit {
      id: string;
      type: PostType;
      mood: MoodCode;
      caption: string;
      excerpt: string;
      linkUrl: string;
      media: MediaItem[];
    }
    ```
  - `ComposeForm({ draft }: { draft?: DraftInit })`

- [ ] **Step 1: Thêm `updateDraft` vào `src/features/compose/actions.ts`**

Đặt ngay sau `saveDraft`:

```ts
/** Sửa nháp: chữ + tâm trạng. Từ chối nếu bài đã đăng. */
export async function updateDraft(
  _prev: ComposeState,
  formData: FormData,
): Promise<ComposeState> {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return { error: "Bạn cần đăng nhập đã nhé." };

  const id = String(formData.get("id") ?? "");
  if (!id) return { error: "Thiếu thông tin bài, thử lại nhé." };

  const existing = await getByIdForAuthor(supabase, id);
  if (!existing || existing.authorId !== user.id)
    return { error: "Không tìm thấy bài." };
  // Bất biến: bài ĐÃ đăng không đi qua đường nháp (kiểm ở SERVER, không chỉ ở UI).
  if (existing.isPublished) return { error: "Bài này đã đăng rồi." };

  const mood = String(formData.get("mood") ?? "");
  if (!MOOD_CODES.includes(mood as MoodCode))
    return { error: "Chọn một tâm trạng giúp mình nhé." };

  const caption = String(formData.get("caption") ?? "").trim();
  const patch: Parameters<typeof updatePost>[2] = {
    mood: mood as MoodCode,
    caption: caption || null,
  };

  if (existing.type === "goc_doc") {
    const excerpt = String(formData.get("excerpt") ?? "").trim();
    const linkUrl = String(formData.get("linkUrl") ?? "").trim();
    if (!linkUrl && !excerpt)
      return { error: "Thêm một link hoặc đoạn trích nhé." };
    if (linkUrl) {
      try {
        new URL(linkUrl);
      } catch {
        return { error: "Link chưa hợp lệ, kiểm lại nhé." };
      }
    }
    patch.excerpt = excerpt || null;
    patch.linkUrl = linkUrl || null;
  }

  try {
    await updatePost(supabase, id, patch);
  } catch {
    return { error: "Chưa lưu được, thử lại nhé." };
  }
  // Nháp không nằm trên trang công khai nào -> không revalidate.
  return { error: null, ok: true };
}
```

- [ ] **Step 2: Cho `ComposeForm` nhận `draft`**

Trong `src/features/compose/ComposeForm.tsx`:

(a) thêm import và export kiểu:

```ts
import { saveDraft, updateDraft } from "./actions";
import type { MediaItem, PostType as DbPostType } from "@/lib/db/types";

export interface DraftInit {
  id: string;
  type: DbPostType;
  mood: MoodCode;
  caption: string;
  excerpt: string;
  linkUrl: string;
  media: MediaItem[];
}
```

Lưu ý: file đang có `type PostType = "khoanh_khac" | "goc_doc" | "hanh_trinh"` khai báo cục bộ — **giữ nguyên** nó, và dùng alias `DbPostType` cho kiểu từ DB để không trùng tên.

(b) đổi chữ ký và state khởi tạo:

```ts
export function ComposeForm({ draft }: { draft?: DraftInit } = {}) {
  const [type, setType] = useState<PostType>(draft?.type ?? "khoanh_khac");
  const [momentKind, setMomentKind] = useState<MomentKind>("image");
  const [updateState, updateAction] = useActionState(updateDraft, initial);
```

(giữ nguyên các `useActionState` khác) và:

```ts
  const [mood, setMood] = useState<MoodCode | "">(draft?.mood ?? "");
```

(c) thêm `updateState.error` vào chuỗi `error`.

(d) ẩn bộ chọn loại bài khi đang sửa nháp. Khối cần bọc là khối JSX **đầu tiên**
trong `<form>`, mở bằng dòng:

```tsx
      <div className="flex gap-1 self-start rounded-full border border-border p-1 text-sm">
```

và chứa `.map` trên mảng `[["khoanh_khac", "Khoảnh khắc"], ["goc_doc", "Góc đọc"], ["hanh_trinh", "Hành trình"]]`.
Chèn `{!draft && (` ngay **trước** dòng mở đó và `)}` ngay **sau** `</div>` đóng
của nó. Nội dung bên trong không sửa một ký tự nào. (Đừng nhầm với khối
`<div className="flex gap-1 self-start rounded-full border border-border p-1 text-xs">`
bên dưới — đó là bộ chọn Ảnh/Video, `text-xs` chứ không phải `text-sm`, và nó
**vẫn phải hiện** khi sửa nháp.)

(e) nạp giá trị ban đầu cho các ô chữ — thêm `defaultValue` vào ba ô:

- `<Input type="url" name="linkUrl" defaultValue={draft?.linkUrl ?? ""} … />`
- `<Textarea name="excerpt" defaultValue={draft?.excerpt ?? ""} … />`
- `<Textarea name="caption" defaultValue={draft?.caption ?? ""} … />`

(f) trong `onSubmit`, nhánh `submitter?.value === "draft"` phải rẽ theo `draft`:

```ts
    if (submitter?.value === "draft") {
      if (draft) {
        // Sửa nháp: KHÔNG gửi media ở task này -> ảnh cũ giữ nguyên.
        const fd = new FormData();
        fd.set("id", draft.id);
        fd.set("mood", mood);
        fd.set("caption", caption);
        if (type === "goc_doc") {
          fd.set("linkUrl", fieldValue(form, "linkUrl"));
          fd.set("excerpt", fieldValue(form, "excerpt"));
        }
        startTransition(() => updateAction(fd));
        return;
      }
      // …nhánh tạo nháp mới của Task 2 giữ nguyên…
    }
```

(g) nhãn nút: khi `draft` thì "Lưu nháp" đổi thành "Lưu thay đổi", và ẩn nút "Đăng" (Đăng làm ở Task 6):

```tsx
      <div className="flex items-center gap-3">
        {!draft && (
          <Button type="submit" value="publish" loading={busy} loadingLabel="Đang lưu…">
            Đăng
          </Button>
        )}
        {!(type === "khoanh_khac" && momentKind === "video") && (
          <Button
            type="submit"
            value="draft"
            variant={draft ? "primary" : "secondary"}
            disabled={busy}
          >
            {draft ? "Lưu thay đổi" : "Lưu nháp"}
          </Button>
        )}
      </div>
```

- [ ] **Step 3: Tạo `src/app/me/nhap/[id]/page.tsx`**

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { getByIdForAuthor } from "@/lib/db/posts";
import { ComposeForm } from "@/features/compose/ComposeForm";

// Nháp chỉ tác giả thấy + đọc session -> không cache.
export const dynamic = "force-dynamic";

export const metadata: Metadata = { title: "Sửa nháp" };

export default async function EditDraftPage({
  params,
}: {
  params: Promise<{ id: string }>; // Next 16: params async
}) {
  const { id } = await params;
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) redirect(`/login?returnTo=/me/nhap/${id}`); // double-check ngoài proxy guard

  const post = await getByIdForAuthor(supabase, id);
  // Đã đăng thì KHÔNG sửa qua đường nháp — bất biến giữ theo cấu trúc route.
  if (!post || post.authorId !== user.id || post.isPublished) notFound();

  return (
    <main className="mx-auto w-full max-w-container px-4.5 py-12">
      <Link href="/me" className="text-sm text-text-muted hover:text-text">
        ← Về trang của bạn
      </Link>
      <h1 className="mt-6 font-serif text-2xl font-medium text-text">
        Sửa nháp
      </h1>
      <div className="mt-8">
        <ComposeForm
          draft={{
            id: post.id,
            type: post.type,
            mood: post.mood,
            caption: post.caption ?? "",
            excerpt: post.excerpt ?? "",
            linkUrl: post.linkUrl ?? "",
            media: post.media,
          }}
        />
      </div>
    </main>
  );
}
```

Không sửa `src/proxy.ts`: `PROTECTED = ["/compose", "/me"]` dùng `startsWith` nên `/me/nhap/[id]` đã được bảo vệ.

- [ ] **Step 4: Chạy gate**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

Trong bảng route của build, `/me/nhap/[id]` phải là `ƒ (Dynamic)`.

- [ ] **Step 5: Kiểm bằng tay**

1. `/me` → bấm một dòng nháp → vào `/me/nhap/<id>`, thấy caption/tâm trạng **đã nạp đúng**, bộ chọn loại bài **không hiện**.
2. Đổi caption + đổi tâm trạng → **Lưu thay đổi** → mở lại thấy giá trị mới.
3. Ảnh của nháp **vẫn còn** trong DB (task này không gửi `media`) — kiểm ở Supabase dashboard cột `media`.
4. Lấy `id` của một bài **đã đăng**, mở `/me/nhap/<id-đó>` → phải ra **404**.
5. Đăng xuất, mở `/me/nhap/<id>` → chuyển sang `/login`.

- [ ] **Step 6: Commit**

```bash
git add src/features/compose/actions.ts src/features/compose/ComposeForm.tsx "src/app/me/nhap/[id]/page.tsx"
git commit -m "feat(nhap): route /me/nhap/[id] + ComposeForm nap nhap (chu + tam trang)"
```

---

### Task 5: Sửa ảnh trong nháp — danh sách hợp nhất

**Đây là task rủi ro cao nhất của cả tính năng.** Form phải quản *ảnh đã nằm trên Storage* và *ảnh mới vừa chọn* trong cùng một danh sách, sao cho xoá / nhập alt / thứ tự hoạt động giống nhau trên cả hai dạng.

**Files:**
- Modify: `src/features/compose/ComposeForm.tsx` (đổi `Picked[]` → `Slot[]`)
- Modify: `src/features/compose/actions.ts` (`updateDraft` nhận `media`, dọn ảnh bị bỏ)

**Interfaces:**
- Consumes: `DraftInit.media` (Task 4), `sanitizeImageMedia`, `mediaPublicUrl`
- Produces:
  - `type Slot = { kind: "existing"; item: MediaItem; alt: string } | { kind: "new"; file: File; url: string; alt: string }` (cục bộ trong `ComposeForm.tsx`)
  - `uploadSlots(list: Slot[]): Promise<MediaItem[] | null>` (thay `uploadPicked`)

- [ ] **Step 1: Đổi `Picked` thành `Slot` trong `ComposeForm.tsx`**

Thay khai báo kiểu và state:

```ts
/**
 * Một ô ảnh trong form. Hai dạng dùng CHUNG mọi thao tác (xoá, nhập alt, thứ tự):
 * - "existing": đã nằm trên Storage (mở lại nháp) — có `path`, không có `File`.
 * - "new": vừa chọn từ máy, chưa resize/upload — có `File` + blob URL.
 */
type Slot =
  | { kind: "existing"; item: MediaItem; alt: string }
  | { kind: "new"; file: File; url: string; alt: string };

/** Ảnh để xem trước: ảnh cũ lấy URL công khai, ảnh mới lấy blob URL. */
function slotPreview(s: Slot): string {
  return s.kind === "existing" ? mediaPublicUrl(s.item.path!) : s.url;
}

/** Khoá React ổn định cho từng ô. */
function slotKey(s: Slot): string {
  return s.kind === "existing" ? `e:${s.item.path}` : `n:${s.url}`;
}
```

Thêm import `mediaPublicUrl`:

```ts
import { mediaPublicUrl } from "@/lib/storage";
```

State khởi tạo từ nháp (chỉ lấy phần tử CÓ `path` — bỏ media video):

```ts
  const [slots, setSlots] = useState<Slot[]>(() =>
    (draft?.media ?? [])
      .filter((m) => !!m.path)
      .map((item) => ({ kind: "existing" as const, item, alt: item.alt ?? "" })),
  );
  const slotsRef = useRef<Slot[]>(slots);
```

- [ ] **Step 2: Cập nhật các hàm quản ảnh**

```ts
  // Giữ ref đồng bộ để cleanup effect đọc bản mới nhất (tránh stale closure).
  useEffect(() => {
    slotsRef.current = slots;
  });

  // Thu hồi blob URL khi unmount — CHỈ ô "new" mới có blob.
  useEffect(
    () => () => {
      slotsRef.current.forEach((s) => {
        if (s.kind === "new") URL.revokeObjectURL(s.url);
      });
    },
    [],
  );

  function onPickFiles(e: React.ChangeEvent<HTMLInputElement>) {
    const files = Array.from(e.target.files ?? []);
    setSlots((prev) => {
      const room = MAX_IMAGES - prev.length;
      const add = files.slice(0, Math.max(0, room)).map(
        (file) =>
          ({
            kind: "new" as const,
            file,
            url: URL.createObjectURL(file),
            alt: "",
          }),
      );
      return [...prev, ...add];
    });
    e.target.value = ""; // cho chọn lại cùng file
  }

  function removeImage(i: number) {
    setSlots((prev) => {
      const s = prev[i];
      if (s?.kind === "new") URL.revokeObjectURL(s.url);
      return prev.filter((_, k) => k !== i);
    });
  }

  function setAlt(i: number, alt: string) {
    setSlots((prev) => prev.map((s, k) => (k === i ? { ...s, alt } : s)));
  }
```

- [ ] **Step 3: `uploadSlots` thay `uploadPicked`**

```ts
  /**
   * Dựng mảng media CUỐI CÙNG theo đúng thứ tự đang hiện trên form:
   * ô "existing" đi qua nguyên vẹn (chỉ cập nhật alt), ô "new" mới resize+upload.
   * Trả null nếu lỗi (đã setLocalError).
   */
  async function uploadSlots(list: Slot[]): Promise<MediaItem[] | null> {
    if (list.length === 0) return [];
    setUploading(true);
    try {
      const supabase = createClient();
      const {
        data: { user },
      } = await supabase.auth.getUser();
      if (!user) {
        setLocalError("Bạn cần đăng nhập đã nhé.");
        return null;
      }
      const media: MediaItem[] = [];
      for (const s of list) {
        if (s.kind === "existing") {
          media.push({ ...s.item, alt: s.alt.trim() || undefined });
          continue;
        }
        let r;
        try {
          r = await resizeImage(s.file);
        } catch {
          setLocalError("Một tấm ảnh chưa xử lý được, thử ảnh khác nhé.");
          return null;
        }
        const path = `${user.id}/${crypto.randomUUID()}.webp`;
        const { error: upErr } = await supabase.storage
          .from("media")
          .upload(path, r.blob, { contentType: "image/webp", upsert: false });
        if (upErr) {
          setLocalError("Chưa tải được ảnh lên, thử lại nhé.");
          return null;
        }
        media.push({
          path,
          w: r.width,
          h: r.height,
          blurDataURL: r.blurDataURL,
          alt: s.alt.trim() || undefined,
        });
      }
      return media;
    } catch {
      setLocalError("Có lỗi khi tải ảnh, thử lại nhé.");
      return null;
    } finally {
      setUploading(false);
    }
  }
```

Rồi xoá hẳn `uploadPicked` và sửa **đúng bảy chỗ** còn đọc `images` (danh sách
đầy đủ, không còn chỗ nào khác):

| Chỗ | Cũ | Mới |
|---|---|---|
| `onSubmit`, nhánh tạo nháp (Task 2) | `const media = images.length > 0 ? await uploadPicked(images) : [];` | `const media = await uploadSlots(slots);` (`uploadSlots` đã tự trả `[]` khi rỗng) |
| `onSubmit`, nhánh `khoanh_khac` ảnh | `if (images.length === 0) return setLocalError("Thêm ít nhất một tấm ảnh nhé.");` | `if (slots.length === 0) …` (giữ nguyên chữ) |
| `onSubmit`, nhánh `khoanh_khac` ảnh | `const media = await uploadPicked(images);` | `const media = await uploadSlots(slots);` |
| `onSubmit`, nhánh `hanh_trinh` | `if (images.length === 0) return setLocalError("Thêm một tấm ảnh cho chặng đầu tiên nhé.");` | `if (slots.length === 0) …` |
| `onSubmit`, nhánh `hanh_trinh` | `if (images.length > 1) return setLocalError("Hành trình mỗi chặng chỉ một ảnh — bỏ bớt nhé.");` | `if (slots.length > 1) …` |
| Lưới ảnh `khoanh_khac` | `{images.length < MAX_IMAGES && (` | `{slots.length < MAX_IMAGES && (` |
| Lưới ảnh `hanh_trinh` | `{images.length < 1 && (` | `{slots.length < 1 && (` |

Sau bước này không còn identifier `images` hay `imagesRef` nào trong file — `npx tsc --noEmit` sẽ bắt nếu sót.

Nhánh sửa nháp trong `onSubmit` giờ **phải** gửi media:

```ts
      if (draft) {
        const media = await uploadSlots(slots);
        if (media === null) return;
        const fd = new FormData();
        fd.set("id", draft.id);
        fd.set("mood", mood);
        fd.set("caption", caption);
        fd.set("media", JSON.stringify(media));
        if (type === "goc_doc") {
          fd.set("linkUrl", fieldValue(form, "linkUrl"));
          fd.set("excerpt", fieldValue(form, "excerpt"));
        }
        if (type === "hanh_trinh") {
          fd.set("note", fieldValue(form, "entryNote"));
          fd.set("date", entryDate || localToday());
        }
        startTransition(() => updateAction(fd));
        return;
      }
```

- [ ] **Step 4: `AltFields` và lưới ảnh dùng `Slot`**

Đổi chữ ký `AltFields`:

```tsx
function AltFields({
  slots,
  onChange,
}: {
  slots: Slot[];
  onChange: (index: number, alt: string) => void;
}) {
  if (slots.length === 0) return null;
  return (
    <div className="flex flex-col gap-2">
      <p className="text-xs text-text-muted">
        Mô tả ảnh (tuỳ chọn) — cho người dùng trình đọc màn hình.
      </p>
      {slots.map((s, i) => (
        <div key={slotKey(s)} className="flex items-center gap-2">
          <span className="h-10 w-10 shrink-0 overflow-hidden rounded-md border border-border">
            {/* eslint-disable-next-line @next/next/no-img-element -- preview (blob hoac URL Storage) */}
            <img
              src={slotPreview(s)}
              alt=""
              className="h-full w-full object-cover"
            />
          </span>
          <Input
            type="text"
            value={s.alt}
            maxLength={MAX_ALT}
            onChange={(e) => onChange(i, e.target.value)}
            aria-label={`Mô tả ảnh ${i + 1}`}
            placeholder={
              slots.length > 1 ? `Trong ảnh ${i + 1} có gì?` : "Trong ảnh có gì?"
            }
            className="flex-1"
          />
        </div>
      ))}
    </div>
  );
}
```

Và trong hai lưới chọn ảnh (nhánh Khoảnh khắc-ảnh và nhánh Hành trình), đổi `images.map((im, i) => …)` thành:

```tsx
                {slots.map((s, i) => (
                  <div
                    key={slotKey(s)}
                    className="relative aspect-square overflow-hidden rounded-md border border-border"
                  >
                    {/* eslint-disable-next-line @next/next/no-img-element -- preview (blob hoac URL Storage) */}
                    <img
                      src={slotPreview(s)}
                      alt=""
                      className="h-full w-full object-cover"
                    />
                    <button
                      type="button"
                      onClick={() => removeImage(i)}
                      aria-label={`Bỏ ảnh ${i + 1}`}
                      className="absolute right-1 top-1 grid h-6 w-6 place-items-center rounded-full bg-surface/90 text-text shadow-soft transition-colors hover:text-accent-text"
                    >×</button>
                  </div>
                ))}
```

và `<AltFields images={images} onChange={setAlt} />` thành `<AltFields slots={slots} onChange={setAlt} />`.

- [ ] **Step 5: `updateDraft` nhận `media` và dọn ảnh bị bỏ**

Trong `src/features/compose/actions.ts`, thêm vào `updateDraft` — **sau** khối `if (existing.type === "goc_doc") {…}`, trước `try { await updatePost(...) }`:

```ts
  // Ảnh: client gửi mảng CUỐI CÙNG (ảnh cũ giữ nguyên path, ảnh mới đã upload).
  // Không gửi field `media` -> giữ nguyên ảnh cũ (đường đi của Task 4).
  const rawMedia = formData.get("media");
  let removedPaths: string[] = [];
  if (rawMedia !== null) {
    let media: MediaItem[] = [];
    try {
      media = sanitizeImageMedia(JSON.parse(String(rawMedia)), user.id);
    } catch {
      return { error: "Danh sách ảnh không hợp lệ, thử lại nhé." };
    }
    if (media.length > MAX_IMAGES) media = media.slice(0, MAX_IMAGES);

    // QUAN TRỌNG: `sanitizeImageMedia` chỉ giữ path/w/h/blurDataURL/alt — nó
    // DROP `date` và `note` của chặng. Với Hành trình phải gắn lại từ form,
    // không thì ngày/ghi chú của chặng rụng mỗi lần lưu nháp. Nháp Hành trình
    // chỉ có MỘT chặng nên cắt về `media[0]` là đúng (xem mục Phạm vi).
    if (existing.type === "hanh_trinh" && media[0]) {
      const meta = sanitizeEntryMeta(formData.get("note"), formData.get("date"));
      media = [{ ...media[0], ...meta }];
    }

    // Ảnh có trong bài cũ mà không còn trong mảng mới -> rác, dọn SAU khi lưu.
    const keep = new Set(media.map((m) => m.path).filter(Boolean));
    removedPaths = existing.media
      .map((m) => m.path)
      .filter((p): p is string => !!p && !keep.has(p));

    patch.media = media;
  }
```

và **sau** khi `updatePost` thành công, trước `return`:

```ts
  // DB đã trỏ danh sách mới -> gỡ ảnh cũ (best-effort; hụt cũng không hỏng bài).
  if (removedPaths.length)
    await supabase.storage.from("media").remove(removedPaths);

  return { error: null, ok: true };
```

- [ ] **Step 6: Chạy gate**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

- [ ] **Step 7: Kiểm bằng tay — kỹ hơn các task khác**

1. `/compose` → lưu nháp 3 ảnh, mỗi ảnh một mô tả khác nhau.
2. Mở `/me/nhap/<id>` → thấy **đủ 3 ảnh** và **3 ô mô tả đã nạp đúng chữ**.
3. Bỏ ảnh giữa (ảnh 2) → thêm 1 ảnh mới + nhập mô tả cho nó → **Lưu thay đổi**.
4. Mở lại → còn 3 ảnh, đúng thứ tự (cũ-1, cũ-3, mới), mô tả đi đúng với từng ảnh (**đây là chỗ dễ lệch nhất**).
5. Supabase Storage → ảnh 2 **đã bị xoá**, hai ảnh cũ còn lại **vẫn còn** (không bị xoá oan).
6. Sửa **chỉ** mô tả, không đổi ảnh → lưu → ảnh không bị upload lại (path không đổi).
7. Bỏ **hết** ảnh → lưu → nháp còn 0 ảnh, không lỗi.
8. Hồi quy: `/compose` đăng một bài 2 ảnh có mô tả (đường không-nháp) → vẫn đúng.

- [ ] **Step 8: Commit**

```bash
git add src/features/compose/ComposeForm.tsx src/features/compose/actions.ts
git commit -m "feat(nhap): sua anh trong nhap (danh sach hop nhat cu+moi, don anh bo)"
```

---

### Task 6: Đăng nháp

**Files:**
- Modify: `src/features/compose/actions.ts` (thêm `publishDraft`)
- Modify: `src/features/compose/ComposeForm.tsx` (nút "Đăng" khi đang sửa nháp)

**Interfaces:**
- Consumes: `publishPost`, `getByIdForAuthor` (Task 1), `updateDraft` (Task 4/5), `uniqueSlug`
- Produces: `publishDraft(prev: ComposeState, formData: FormData): Promise<ComposeState>`

- [ ] **Step 1: Thêm `publishDraft` vào `src/features/compose/actions.ts`**

Đặt sau `updateDraft`. Hàm này **lưu thay đổi trước rồi mới đăng** — nếu chỉ đăng thì chữ vừa sửa trên form sẽ mất:

```ts
/**
 * Đăng một nháp: lưu thay đổi đang có trên form TRƯỚC (nếu không, chữ vừa sửa
 * mà chưa bấm "Lưu thay đổi" sẽ mất), rồi sinh slug thật và bật is_published.
 */
export async function publishDraft(
  prev: ComposeState,
  formData: FormData,
): Promise<ComposeState> {
  const saved = await updateDraft(prev, formData);
  if (saved.error) return saved; // lưu hụt -> KHÔNG đăng

  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) return { error: "Bạn cần đăng nhập đã nhé." };

  const id = String(formData.get("id") ?? "");
  const existing = await getByIdForAuthor(supabase, id); // đọc lại: đã có chữ mới
  if (!existing || existing.authorId !== user.id)
    return { error: "Không tìm thấy bài." };
  if (existing.isPublished) return { error: "Bài này đã đăng rồi." };

  // Đăng thì mới bắt buộc có nội dung (nháp được phép còn thiếu).
  if (existing.type === "goc_doc") {
    if (!existing.linkUrl && !existing.excerpt)
      return { error: "Thêm một link hoặc đoạn trích nhé." };
  } else if (existing.media.length === 0) {
    return { error: "Thêm ít nhất một tấm ảnh trước khi đăng nhé." };
  }

  // Slug thật — cùng quy tắc với luồng tạo: Góc đọc lấy trích trước, còn lại caption.
  const hint =
    existing.type === "goc_doc"
      ? existing.excerpt || existing.caption || ""
      : existing.caption || "";
  const prefix =
    existing.type === "goc_doc"
      ? "goc-doc"
      : existing.type === "hanh_trinh"
        ? "hanh-trinh"
        : "khoanh-khac";
  const slug = await uniqueSlug(supabase, hint, prefix);

  try {
    await publishPost(supabase, existing.id, slug);
  } catch {
    return { error: "Chưa đăng được, thử lại nhé." };
  }

  revalidatePath("/"); // ISR: bài mới phải xuất hiện ở Feed NGAY
  revalidatePath(`/m/${slug}`);
  redirect(`/m/${slug}`);
}
```

- [ ] **Step 2: Nút "Đăng" khi đang sửa nháp**

Trong `src/features/compose/ComposeForm.tsx`:

(a) thêm `publishDraft` vào import từ `./actions` và thêm:

```ts
  const [publishState, publishAction] = useActionState(publishDraft, initial);
```

(b) thêm `publishState.error` vào chuỗi `error`.

(c) trong `onSubmit`, khi **có** `draft` và nút bấm **không** phải `draft`, dùng `publishAction` với đúng FormData như nhánh sửa nháp. Gộp lại cho khỏi trùng code — thay cả nhánh `if (draft)` bằng:

```ts
    if (draft) {
      const media = await uploadSlots(slots);
      if (media === null) return;
      const fd = new FormData();
      fd.set("id", draft.id);
      fd.set("mood", mood);
      fd.set("caption", caption);
      fd.set("media", JSON.stringify(media));
      if (type === "goc_doc") {
        fd.set("linkUrl", fieldValue(form, "linkUrl"));
        fd.set("excerpt", fieldValue(form, "excerpt"));
      }
      if (type === "hanh_trinh") {
        fd.set("note", fieldValue(form, "entryNote"));
        fd.set("date", entryDate || localToday());
      }
      const act = submitter?.value === "draft" ? updateAction : publishAction;
      startTransition(() => act(fd));
      return;
    }
```

Nhánh này nằm **trước** `if (submitter?.value === "draft") {…}` của Task 2 (nhánh tạo nháp mới), vì khi có `draft` thì cả hai nút đều đi qua đây.

(d) hiện lại nút Đăng khi sửa nháp — bỏ điều kiện `!draft` ở nút Đăng:

```tsx
      <div className="flex items-center gap-3">
        <Button type="submit" value="publish" loading={busy} loadingLabel="Đang lưu…">
          Đăng
        </Button>
        {!(type === "khoanh_khac" && momentKind === "video") && (
          <Button
            type="submit"
            value="draft"
            variant="secondary"
            disabled={busy}
          >
            {draft ? "Lưu thay đổi" : "Lưu nháp"}
          </Button>
        )}
      </div>
```

- [ ] **Step 3: Chạy gate**

```bash
npm run lint
npx tsc --noEmit
npm run build
```

- [ ] **Step 4: Kiểm bằng tay — vòng đầy đủ**

1. `/me/nhap/<id>` → sửa caption thành `Về quê ăn giỗ` **rồi bấm Đăng luôn** (không bấm Lưu thay đổi trước).
2. Phải chuyển sang `/m/ve-que-an-gio` — **slug khớp caption mới**, không còn dạng `nhap-…`.
3. Bài xuất hiện ở `/` ngay (đã `revalidatePath`), có ở `/lich`, và ở `/sitemap.xml`.
4. Caption trên trang bài là chữ vừa sửa (chứng minh `publishDraft` lưu trước khi đăng).
5. `/me` → khu "Nháp" giảm 1, dải thống kê "N bài" tăng 1.
6. Mở `/me/nhap/<id>` của bài vừa đăng → **404**.
7. Tạo nháp Khoảnh khắc **không có ảnh** → bấm Đăng → báo "Thêm ít nhất một tấm ảnh trước khi đăng nhé.", bài vẫn là nháp.
8. Mở `/m/<slug-mới>` ở cửa sổ ẩn danh → thấy bài (đã công khai).

- [ ] **Step 5: Commit**

```bash
git add src/features/compose/actions.ts src/features/compose/ComposeForm.tsx
git commit -m "feat(nhap): dang nhap (luu thay doi truoc, sinh slug that tu caption)"
```

---

## Kiểm chứng cuối (sau Task 6)

- [ ] `npm run lint` sạch
- [ ] `npx tsc --noEmit` sạch
- [ ] `npm run build` pass — bảng route: `/` vẫn `○` revalidate 5m, `/me/nhap/[id]` là `ƒ`
- [ ] **Phép thử bảo mật**: một nháp còn sống, mở `/m/<slug-tạm>` ở cửa sổ ẩn danh → 404
- [ ] Không có bất biến nào bị phá: tim vẫn ẩn danh, `heart_counts` không đổi, danh sách cột trong `src/lib/db/comments.ts` không đổi, không hex tâm trạng hardcode, không spinner quay
- [ ] Không lộ secret (nháp không thêm env nào)

## Hạn chế đã biết (đã chấp nhận trong spec)

- Nháp bị bỏ quên nằm đó mãi, ảnh của nó vẫn tính vào quota Storage. Không có job dọn định kỳ — job tự xoá bài người ta viết dở là hành vi tệ hơn.
- `slugExists` chỉ thấy nháp của chính tác giả (RLS); ràng buộc `slug unique` ở DB là chốt cuối.
- Nháp không hỗ trợ Khoảnh khắc-video (quyết định ở tầng plan, xem mục Phạm vi).
